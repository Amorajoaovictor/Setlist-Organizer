kind = "web"
previewPath = "/"
title = "Band Setlist Organizer"
version = "1.0.0"
id = "artifacts/setlist-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20880

[services.development]
run = "pnpm --filter @workspace/setlist-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/setlist-app", "run", "build" ]
publicDir = "artifacts/setlist-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20880"
BASE_PATH = "/"
